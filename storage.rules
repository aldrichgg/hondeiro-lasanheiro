rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write for authenticated users
      // In a production app, we would restrict to specific folders per user
      allow read, write: if request.auth != null;
    }
  }
}
